<h1>Hello Client World</h1>

 <% flash.each do |name,msg| %>
    <%= msg %></br>
 <% end %>

<ul>
	<% @clients.each do |client| %>
		<li>
			<%=link_to "#{client.first_name} #{client.last_name} #{client.email} #{client.company_name}" , client%>
		</li>
	<%end%>
</ul>

